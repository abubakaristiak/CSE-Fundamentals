#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr[2];
    
    arr[0].push_back(1);
    arr[0].push_back(2);
    arr[0].push_back(3);
    arr[0].push_back(4);
    
    arr[1].push_back(1);
    arr[1].push_back(2);
    arr[1].push_back(3);
    arr[1].push_back(4);

    // arr.push_back(vector<int>());

    //  for(int i=0; i<arr.size(); i++){       // akane array row size fixed so, size holo vector stl. it's not work proper.
    //     for(int j=0; j<arr[i].size(); j++){
    //         cout << arr[i][j] << " ";
    //     }
    //     cout << endl;
    // }
    return 0;
}